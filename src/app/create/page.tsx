export default function CreatePage() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center min-h-screen px-6">
      <h1 className="text-5xl text-ink" style={{ fontFamily: "var(--font-display)" }}>
        pick your flowers.
      </h1>
      <p className="mt-4 text-[var(--color-ink-muted)]" style={{ fontFamily: "var(--font-body)" }}>
        coming soon — 12 flowers, endless combinations.
      </p>
    </main>
  );
}
